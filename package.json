{
  "name": "sunburnt",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "dev": "npx shadow-cljs compile worker client && concurrently -n cljs,worker -c blue,green \"npx shadow-cljs watch worker client\" \"npx wrangler dev\"",
    "watch": "npx shadow-cljs watch worker client",
    "release": "npx shadow-cljs release worker client",
    "wrangler": "npx wrangler dev",
    "deploy": "npx wrangler deploy"
  },
  "devDependencies": {
    "concurrently": "^9.2.1",
    "shadow-cljs": "^2.28.0"
  },
  "dependencies": {
    "saxes": "^6.0.0",
    "wrangler": "^4.54.0"
  }
}
